# Server Binaries & Runtimes
servers/
bedrock-server-*.zip
*.mcworld

# Configs containing secrets/personal info
# (Note: We are keeping the templates in system/, but excluding live configs if they exist elsewhere)
allowlist.json
permissions.json

# Temporary & Cache
tmp/
cache/
.gemini/
__pycache__/
*.log

# Archives
_archive/
_scripts_old/
system/preview_bin/
system/stable_bin/
